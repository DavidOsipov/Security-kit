import { describe, it, expect } from "vitest";

// Placeholder suite to satisfy runner; real IDNA integration is covered elsewhere.
describe("url.idna.integration placeholder", () => {
  it("noop", () => {
    expect(true).toBe(true);
  });
});
